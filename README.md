# Cmd plugin for Allow2Automate

Extend Allow2Automate with the ability to run scripts and commands on the local machine

# Examples

The following are examples of how you can use this plugin to perform certain tasks:
To use any of these options, you only need to have allow2automate running and have loaded the allow2automate-cmd plugin.

Just bear in mind, it is not intended that this is run in the context of a child account. So this is more intended to trigger
a local function on perhaps a parent owned machine, that will then

## 1. shutdown the local machine if time runs out

### Windows
```bat
???
```

### Mac or Linux
```bash
sudo shutdown now
```

## 1. shutdown the local machine if time runs out
